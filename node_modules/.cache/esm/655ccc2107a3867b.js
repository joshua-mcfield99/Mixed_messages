let object;_07a‍.w("./object.js",[["default",["object"],function(v){object=v}]]);

const authorQuote = document.getElementById("author-quote");
const authorName = document.getElementById("author-name");
const authorDate = document.getElementById("author-date");
const button = document.getElementById("btn");


const selector = (arr) =>{
    const index = Math.floor(Math.random()*arr.length);
    let quote = "", author = "", date="";
    if(arr === object.quotes) {
        quote = arr[index];
        return quote;
    } else if (arr === object.authors) {
        author = arr[index];
        return author;
    } else if (arr === object.dates){
        date = arr[index];
        return date;
    } else {
        return 'Try again';
    };
    
}

const quote = selector(object.quotes), date = selector(object.dates), authour = selector(object.authors);

button.addEventListener("click") = function(){
    authorQuote.innerHTML = quote;
}
_07a‍.g.console.log(selector(object.quotes));
_07a‍.g.console.log(selector(object.dates));
_07a‍.g.console.log(selector(object.authors));